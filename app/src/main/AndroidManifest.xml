<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.codingxproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".UserAreaActivity"></activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".DrugsInfoActivity" />
        <activity android:name=".DrugCard" />
        <activity android:name=".DetailDrugInfoActivity" />
        android:theme="@style/AppTheme">
        <activity android:name=".DataRecord_BloodSugar"></activity>
        <activity android:name=".DataRecord_BloodPressure_DBP" />
        <activity android:name=".DataRecord_BloodPressure_SBP" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- remind_record activity -->
        <activity android:name=".RemindRecordActivity" /> <!-- RemindDrugTakingActivity activity -->
        <activity android:name=".RemindDrugTakingActivity" /> <!-- remind_takedMedicine -->
        <activity android:name=".RemindTakeMedActivity" /> <!-- SetTimeActivity -->
        <activity android:name=".SetTimeActivity" /> <!-- DrugTimePageActivity activity -->
        <activity android:name=".DrugTimePageActivity" /> <!-- DrugTime activity -->
        <!-- DateRecord activity -->
        </activity> <!-- remind_record activity -->
        <activity android:name=".remind_Record" /> <!-- remind_DrugTaking activity -->
        <activity android:name=".remind_DrugTaking" /> <!-- remind_takedMedicine -->
        <activity android:name=".remind_takeMedicine" /> <!-- set_time -->
        <activity android:name=".set_time" /> <!-- drug_time_page activity -->
        <activity android:name=".drug_time_page" /> <!-- drug_time activity -->
        <activity android:name=".drug_time" /> <!-- DateRecord activity -->
        <activity
            android:name=".DataRecordActivity"
            android:label="@string/title_activity_data_record"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>